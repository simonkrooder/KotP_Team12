
# Copy this file to .env and fill with your Azure project and credential details.
# The real .env is ignored by git (.gitignore) and should contain secrets.

# Name of the deployed agent/model (deployment name)
AGENT_MODEL_DEPLOYMENT_NAME=your-deployment-name

# (Optional) Name of the deep research model deployment
DEEP_RESEARCH_MODEL_DEPLOYMENT_NAME=your-deep-research-deployment

# Azure AI project endpoint URL, e.g. https://<resource-name>.<region>.api.cognitive.microsoft.com
PROJECT_ENDPOINT=https://your-project-endpoint

# Azure subscription ID and resource group
AZURE_SUBSCRIPTION_ID=your-subscription-id
AZURE_RESOURCE_GROUP_NAME=your-resource-group

# (Optional) Azure project name
AZURE_PROJECT_NAME=your-azure-project-name

# (Optional) temperature for the agent (float)
AGENT_TEMPERATURE=0.2

# (Optional) MCV server URL (default: http://localhost:8000)
MCV_SERVER_URL=http://localhost:8000

# For developer authentication, you can rely on DefaultAzureCredential which supports
# environment-based credentials (e.g., AZURE_CLIENT_ID, AZURE_TENANT_ID, AZURE_CLIENT_SECRET)
# or managed identity when running in Azure.
# Example client credentials (only use for local dev and never commit real secrets):
# AZURE_CLIENT_ID=your-client-id
# AZURE_TENANT_ID=your-tenant-id
# AZURE_CLIENT_SECRET=your-client-secret
